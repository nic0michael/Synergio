# H2 database
## console URL
**[http://localhost:8088/h2-console](http://localhost:8088/h2-console)**

## The JDBC URL to use in the H2 Console login screen is:

**`jdbc:h2:file:./data/database`**

(Ensure you select **Generic H2 (Embedded)** if asked for the saved settings, but usually just pasting the URL is enough).

**Login Details:**
*   **Driver Class:** `org.h2.Driver`
*   **JDBC URL:** `jdbc:h2:file:./data/database`
*   **User Name:** `sa`
*   **Password:** *(leave blank)*